import { Canvas, Meta, Story } from "@storybook/blocks";

import * as MarkdownTaskList from "../markdown/TaskListmarkdown.stories";
import * as taskList from "./task-list.stories";

<Meta of={taskList} />

# Simple Taskcomponent

Let's begin by creating a basic task component and then writing a story for it.

## Flowchart

<Story of={MarkdownTaskList.PrivateMarkdownTlist} />
